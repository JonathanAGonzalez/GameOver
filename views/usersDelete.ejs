<!DOCTYPE html>
<html lang="es">
  <%- include('./partials/head') %>
  <body>
    <div class="container-fluid">
      <!--HEADER-->
      <%- include('./partials/header') %> 
      <!--FIN HEADER-->

      <!--MAIN-->
      <div class="row ">
      <%- include('./partials/sideBar') %>

      <div class="products col-10">
        <h1>Listado de usuarios</h1>
        <table class="table table-striped table-dark mt-4">
          <thead>
            <!--TITULO-->
            <tr>
              <th scope="col">ID</th>
              <th scope="col">Nombre</th>
              <th scope="col">Apellido</th>
              <th scope="col">Email</th>
              <th scope="col">Estado</th>
              <th scope="col">Administrador</th>
              <th scope="col">Editar</th>
            </tr>
          </thead>
    
          <% users.forEach(user => { %>
            <form action="/admin/usersDelete/<%= user.id %>" method="post">
          <tbody>
            <tr>
              <th scope="row"><%= user.id %> </th>
              <td><%= user.first_name %> </td>
              <td><%= user.last_name %> </td>
              <td><%= user.email %> </td>
              <td>
                <select name="state">
                  <option value="Inactive" <% if (user.state == "Inactive"){%>selected = "selected"<% } %> >Inactive</option>
                    <option value="Active" <% if (user.state == "Active"){%>selected = "selected"<% } %> >Active</option>
                  </select>
                   </td>
                   <td><% if (user.admin == true) { %>
                    <strong style="color:green"><%= user.admin %></strong>
                   <% } else{%>
                    <strong style="color:red"><%= user.admin %></strong>
                    <%}%>
                </td>
              <td>
                
                    <button class="btn btn-success" type="submit">CONFIRMAR</button>
                </form>
              </td>
            </tr>
          </tbody>
          <% }) %>
    
    
        </table>
      </div>
    </div>
    <!--FIN MAIN-->

    <!--FOOTER-->
      <%- include('./partials/footer') %>
    </div>
    <!--SCRIPT-->

    <!--Optional JavaScript-->
    <!--jQuery first, then Popper.js, then Bootstrap JS-->
    <%- include('./partials/scripts') %> 
  </body>
</html>